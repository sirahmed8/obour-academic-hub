{
  "instructions": "START: Read AI_STATUS.md (live handoff). Protocol: AI_INSTRUCTIONS.md. Before any code, architecture, config, or documentation changes, read AI_STATUS.md in the repository root. Treat it as the single source of truth for multi-agent coordination. After finishing and verifying a task, update AI_STATUS.md with changed files, verification, remaining risks, and next steps. Preserve other agents' unrelated work. When editing the authentication flow in 'src/contexts/AuthContext.tsx' or 'src/components/features/LoginScreen.tsx', ALWAYS use popup-based login (signInWithPopup). NEVER switch back to signInWithRedirect as it causes production failures. Documented in PROJECT_GUIDELINES.md."
}
